import './App.css';

import { addDocument, collection  } from 'firebase/firestore';
import { getAuth, singInAnonymously } from 'firebase/auth';

function App() {
  return (
    <div className="App">
      <header className="App-header">
        Brilliant React Code
      </header>
    </div>
  );
}

export default App;
